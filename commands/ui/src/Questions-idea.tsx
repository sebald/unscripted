/**
 * Container components with in "inquirere like" API
 *
 * ```jsx
 * const questions = [{
 *  type: 'text',
 *  label: 'label',
 *  validate: () => {}
 * }, {
 *  ...
 * }]
 *
 * <Questions questions={questions}/>
 * ```
 * Or is that a hook? No, needs to render something.
 */

/**
 * Question component that renders the question "type"
 *
 * ```jsx
 * <Question kind="text" />
 * <Question kind="select" />
 * ```
 *
 * The <Field/> is Basically the Text questions!
 */
